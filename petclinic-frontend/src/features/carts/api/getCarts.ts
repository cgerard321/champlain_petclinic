/*import axios from 'axios';

const BASE_URL = '/api/v2/gateway/carts';

export const getAllCarts = async () => {
    try {
        const response = await axios.get(`${BASE_URL}`);
        return response.data;
    } catch (error) {
        console.error('Error fetching carts:', error);
        throw error;
    }
};*/