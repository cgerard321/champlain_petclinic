server:
  port: 7007

  error:
    include-message: always

logging:
  level:
    root: INFO
    com.petclinic: DEBUG

app:
  auth-service:
    host: localhost
    port: 7005
  emailing-service:
    host: localhost
    port: 5115

---
# for test

spring:
  config:
    activate:
      on-profile: test

  data:
    mongodb:
      username: root
      password: password
      host: localhost
      port: 0
      database: products
      authentication-database: admin
      auto-index-creation: true

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 4.4.0

---

spring:
  codec:
    max-in-memory-size: 16MB
  config:
    activate:
      on-profile: docker

  data:
    mongodb:
      host: mongo-products
      port: 27017
      database: products
      username: root
      password: password
      authentication-database: admin

app:
  auth-service:
    host: auth
    port: 8080
  emailing-service:
    host: mailer-service
    port: 8080

server.port: 8080