server.port: 7001

server:
  error:
    include-message: always

logging:
  level:
    root: INFO
    com.petclinic: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE

---
# recommend setting this to "none" in a production environment
# spring.jpa.hibernate.ddl-auto: none
#h2 profile
spring:
  config:
    activate:
      on-profile: h2
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:vet-db;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    defer-datasource-initialization: true
  sql:
    init:
      platform: h2
      mode: always

#for real
#spring.datasource:
#  url: jdbc:mysql://localhost/petclinic
#  username: pc
#  password: pc

spring.datasource.hikari.initializationFailTimeout: 60000

---
spring:
  datasource:
    url: jdbc:mysql://mysql6/vet-db
    username: user
    password: pwd

  # recommend setting this to "none" for production environments

  jpa:
    hibernate:
      ddl-auto: none
  config:
    activate:
      on-profile: docker
  sql:
    init:
      platform: mysql
      mode: always

spring.datasource.hikari.initializationFailTimeout: 60000

server.port: 8080