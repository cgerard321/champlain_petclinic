server:
  port: 7008
  error:
    include-message: always

logging:
  level:
    root: INFO
    com.petclinic: DEBUG

spring:
  config:
    activate:
      on-profile: default
  data:
    mongodb:
      port: 0
      database: carts

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 5.0.5

---

spring:
  config:
    activate:
      on-profile: test
  data:
    mongodb:
      port: 0
      database: carts
  flapdoodle:
    mongodb:
      embedded:
        version: 5.0.5

---

spring:
  config:
    activate:
      on-profile: docker
management:
  endpoints:
    web:
      exposure:
        include: health, prometheus
  data:
    mongodb:
      host: mongo-carts
      port: 27017
      database: carts
      username: root
      password: password
      authentication-database: admin

server:
  port: 8080
  error:
    include-message: always

---

spring:
  config:
    activate:
      on-profile: local
  data:
    mongodb:
      host: localhost
      port: 27022
      database: carts
      username: root
      password: password
      authentication-database: admin
