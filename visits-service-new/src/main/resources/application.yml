server:
  port: 8080

logging:
  level:
    root: INFO
    com.pet-clinic: DEBUG

spring:
  config:
    activate:
      on-profile: default
  data:
    mongodb:
      port: 0
      database: visits
  mongodb:
    embedded:
      version: 4.0.21

---
spring:
  config:
    activate:
      on-profile: integration
  data:
    mongodb:
      host: localhost
      port: 27017
      database: visits
      username: root
      password: password
      authentication-database: admin
---
spring:
  config:
    activate:
      on-profile: docker
  data:
    mongodb:
      host: visits-service-new
      port: 27020
      database: visits
      username: root
      password: password
      authentication-database: admin